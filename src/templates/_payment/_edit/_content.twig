{{ hiddenInput('paymentId', element.id) }}

<div id="fields">
    {{ form.render()|raw }}
</div>

<hr>
<div>
    <h2>{{ "Transactions"|t }}</h2>
    {% include 'mollie-payments/_includes/_transactions.twig' with {
        transactions : transactions,
        element: element,
        subscription: false,
    } only %}
</div>

{% hook 'cp.payments.edit.content' %}