import{_ as o,C as l,o as r,c as d,j as s,a as t,E as i,ae as n}from"./chunks/framework.Bry8VeCl.js";const v=JSON.parse('{"title":"Console commands","description":"","frontmatter":{},"headers":[],"relativePath":"console-commands.md","filePath":"console-commands.md"}'),c={name:"console-commands.md"},p={id:"recover-stuck-subscriptions",tabindex:"-1"},h={id:"backfill-missing-payment-dates",tabindex:"-1"};function u(m,e,g,k,b,y){const a=l("Badge");return r(),d("div",null,[e[6]||(e[6]=s("h1",{id:"console-commands",tabindex:"-1"},[t("Console commands "),s("a",{class:"header-anchor",href:"#console-commands","aria-label":'Permalink to "Console commands"'},"​")],-1)),e[7]||(e[7]=s("p",null,[t("The plugin ships a few maintenance commands, run from your project root with "),s("code",null,"./craft"),t(". They exist to repair data that a bug left in a bad state — Mollie does not resend webhooks for old payments, so those cases have to be fixed manually.")],-1)),e[8]||(e[8]=s("p",null,[t("Every command supports "),s("code",null,"--dry-run"),t(", which reports what it would do without writing anything or contacting Mollie to create anything. Always run that first.")],-1)),s("h2",p,[e[0]||(e[0]=t("Recover stuck subscriptions ",-1)),i(a,{type:"info",text:"5.4.5"}),e[1]||(e[1]=t()),e[2]||(e[2]=s("a",{class:"header-anchor",href:"#recover-stuck-subscriptions","aria-label":'Permalink to "Recover stuck subscriptions <Badge type="info" text="5.4.5" />"'},"​",-1))]),e[9]||(e[9]=n(`<p>If a subscription&#39;s first payment was paid but the subscription was never created in Mollie, it stays stuck: its status remains <code>pending</code> and no <code>subscriptionId</code> is stored.</p><p>A subscription is considered stuck when <strong>all</strong> of the following are true:</p><ul><li><code>subscriptionStatus</code> is <code>pending</code></li><li>it has no <code>subscriptionId</code></li><li>it has a linked transaction with status <code>paid</code></li></ul><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># List what would be recovered, without contacting Mollie to create anything</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./craft</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mollie-payments/subscriptions/recover</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --dry-run</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Recover the stuck subscriptions</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./craft</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mollie-payments/subscriptions/recover</span></span></code></pre></div><p>The <code>--dry-run</code> is read-only and prints, per subscription, the date the first charge would be scheduled on (or whether it would be linked to an existing Mollie subscription).</p><h3 id="first-charge-date" tabindex="-1">First charge date <a class="header-anchor" href="#first-charge-date" aria-label="Permalink to &quot;First charge date&quot;">​</a></h3><p>The first charge is anchored to the original billing cadence: it&#39;s set to the first occurrence of <code>paidAt + N × interval</code> that falls today or later. This keeps the customer&#39;s billing day consistent and never charges for an already-paid period.</p><div class="warning custom-block github-alert"><p class="custom-block-title">WARNING</p><p>Mollie does not allow a <code>startDate</code> in the past, so any billing cycles that elapsed while the subscription was stuck cannot be reclaimed. The first recovered charge is the next upcoming cycle.</p></div><h3 id="avoiding-duplicates" tabindex="-1">Avoiding duplicates <a class="header-anchor" href="#avoiding-duplicates" aria-label="Permalink to &quot;Avoiding duplicates&quot;">​</a></h3><p>The command guards against duplicates on several levels:</p><ul><li><strong>Customer already subscribed</strong> — if the customer already has a non-canceled subscription (with a <code>subscriptionId</code>) for the same form + amount + interval, the stuck records are failed duplicates of it. They are marked <code>canceled</code> and nothing is created. This is checked against the local database, which stays reliable even when the Mollie API lookup misses the existing subscription.</li><li><strong>Repeated signup attempts</strong> — a stuck first payment often led customers to try again, leaving several <code>pending</code> subscriptions for the same person. The command groups stuck subscriptions by form + email + amount + interval and only recovers the one with the most recent payment; the rest are marked <code>canceled</code>. This way a customer ends up with exactly one active subscription and is never billed twice.</li><li><strong>Already created in Mollie</strong> — as a final net before creating, it checks Mollie for an existing, non-canceled subscription for that customer (amount + currency + interval). If one is found, the command links to it instead of creating a duplicate.</li></ul><h3 id="excluding-addresses" tabindex="-1">Excluding addresses <a class="header-anchor" href="#excluding-addresses" aria-label="Permalink to &quot;Excluding addresses&quot;">​</a></h3><p>Use <code>--exclude</code> to skip specific email addresses entirely — handy for test accounts:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./craft</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mollie-payments/subscriptions/recover</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --exclude=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;test@example.com,another@example.com&quot;</span></span></code></pre></div><p>The command is safe to run multiple times: once a subscription has a <code>subscriptionId</code> it is no longer selected.</p>`,15)),s("h2",h,[e[3]||(e[3]=t("Backfill missing payment dates ",-1)),i(a,{type:"info",text:"5.4.9"}),e[4]||(e[4]=t()),e[5]||(e[5]=s("a",{class:"header-anchor",href:"#backfill-missing-payment-dates","aria-label":'Permalink to "Backfill missing payment dates <Badge type="info" text="5.4.9" />"'},"​",-1))]),e[10]||(e[10]=n(`<p>Recurring charges are created by Mollie&#39;s own subscription engine, so the plugin has no local transaction for them when their webhook comes in. Before 5.4.9 that path stored the charge with <code>status = paid</code> but never filled in <code>paidAt</code> or <code>method</code>, so anything that treats a truthy <code>paidAt</code> as &quot;this was paid&quot; (exports, reports) silently skipped those charges.</p><p>The fix only applies to charges from 5.4.9 onwards. To repair the ones already stored:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># List what would be backfilled, without writing anything</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./craft</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mollie-payments/transactions/backfill-paid-at</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --dry-run</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Backfill paidAt and method</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./craft</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mollie-payments/transactions/backfill-paid-at</span></span></code></pre></div><p>It selects every transaction with <code>status = paid</code> and no <code>paidAt</code>, re-fetches the payment from Mollie and stores the <code>paidAt</code> and <code>method</code> Mollie reports.</p><p>Transactions Mollie no longer reports as paid are left untouched and listed separately, so the command is safe to run multiple times. Transactions whose element can no longer be found are reported as failures and the command exits with a non-zero status; everything else is still processed.</p>`,5))])}const w=o(c,[["render",u]]);export{v as __pageData,w as default};
